A staff member wants a quick way to edit files
    A staff member finds a post processor that outputs files in a certain manner, he wants to create a
    macro that can find and replace/delete certain lines of code.

A staff member wants a quick way to frame his/her files before running them.
    The person loads up their file, selects their machine and overrides their coordinate system
    if needed.

The fablab gets a new machine and want's to add it
    There is a new machine, it needs it's parameters entered in. The bed size,
    z limit, and command to turn off the spindle are all input.


